# CLAUDE.md — AIコンサルタント HP プロジェクト設定ファイル

## このプロジェクトについて

このプロジェクトは、AIコンサルタント・Take（Takeshi）の自己紹介兼サービス紹介HPを構築するプロジェクトです。
ターゲットは「AIを使いたいが、どこから始めればいいか分からない中小企業オーナー・非エンジニア経営者」です。

---

## 制作者プロフィール（必ず参照すること）

- **名前**：Take（Takeshi）／ 株式会社Sum-Sets 代表
- **背景**：営業・コーチング出身。自らのペットホテル「ワンルーク江東区亀戸店」でAI導入を実践し、その実績を元にAIコンサルティング事業を展開
- **強み**：「自分自身がAIを使いこなせるようになった非エンジニア」という視点。技術を難しく語らず、現場目線で伝えられる
- **実績（証拠）**：
  - ワンルーク亀戸店にLINEチャットボット・業務マニュアル・採用資料をAIで構築
  - Claude Code・Dify・Make・pptxgenjs・GitHub Pages・Obsidianを実務で活用
  - AIコンサル・セミナー事業を立ち上げ中
- **提供サービス**：
  - AI導入コンサルティング（中小企業向け）
  - AIセミナー・勉強会の開催
  - Claude Code・Dify・Makeを使った業務自動化の実装支援
- **ターゲット**：AIに興味があるが何から始めればいいか分からない中小企業オーナー、店舗経営者、個人事業主
- **活動拠点**：東京都江東区（亀戸エリア）

---

## デザインルール（必ず守ること）

- **ブランドカラー**：
  - メイン：`#0ABAB5`（ティール）→ Tailwind utility: `brand`
  - アクセント：`#1a1a2e`（ディープネイビー）→ utility: `navy`
  - 背景ベース：`#ffffff`（白）／ `#f8fafc`（薄いグレー）→ utility: `paper`
- **デザイン方向**：モダン・クリーン・信頼感。派手すぎず、技術系すぎず。「話しかけやすいプロ」のイメージ
- **フォント方針**：日本語は Noto Sans JP。英語は Inter
- **写真・画像**：プレースホルダーは unsplash 系の高品質フリー素材を使用
- **アイコン**：lucide-react

---

## 技術ルール（必ず守ること）

- **フレームワーク**：Next.js 16（App Router）
- **スタイリング**：Tailwind CSS v4（@theme でブランドカラー定義）
- **言語**：TypeScript
- **レスポンシブ**：モバイルファースト（sm: md: lg:）。スマホ375pxで崩れないことを最優先
- **コメント**：日本語
- **返答**：Claude Codeからの返答は日本語
- **チャットボット**：Anthropic Claude API（claude-sonnet-4-6）

---

## ディレクトリ構成

```
take-consulting-hp/
├── app/
│   ├── layout.tsx        # フォント・SEO・Header/Footer/ChatWidget配置
│   ├── page.tsx          # 全セクション統合
│   ├── globals.css       # Tailwind読み込み + ブランドトークン
│   └── api/chat/route.ts # チャットボットAPI（Claude API）
├── components/           # 各セクションコンポーネント
├── data/                 # ★コンテンツ（後から編集可能）
│   ├── results.ts
│   ├── services.ts
│   ├── beforeAfter.ts
│   ├── forWhom.ts
│   ├── faqs.ts
│   ├── nav.ts
│   └── chatbotPrompt.ts
├── lib/anthropic.ts      # Anthropic SDK
├── .env.local            # ANTHROPIC_API_KEY（コミット禁止）
└── .env.example
```

### コンテンツの編集方法
- 数字・サービス・FAQ・事例 を変更したいときは `data/*.ts` の配列を編集してください。
- コンポーネント側を触らず、配列に項目を足す/減らすだけで増減OK。

---

## HP の必須セクション構成

以下のセクションを必ず含める。順番も基本的にこの順で配置する：

1. Hero（キャッチコピー・サブ・CTA×2・信頼バッジ）
2. Results（実績・数字 4枚）
3. Services（提供サービス 3つ）
4. Before / After（ワンルーク亀戸店の実例）
5. For Whom（こんな方に）
6. About（自己紹介）
7. FAQ
8. Contact（お問い合わせフォーム）
9. ChatWidget（右下固定）

---

## チャットボットのシステムプロンプト

`data/chatbotPrompt.ts` に定義。変更したいときはここを編集。

---

## 品質チェック基準

- [ ] スマホ（375px）で表示が崩れていないか
- [ ] 全セクションが正しい順序で表示されているか
- [ ] チャットボットが右下に固定されているか
- [ ] CTAボタンが目立つ色（brand `#0ABAB5`）になっているか
- [ ] フォームが正常に動作するか（モック動作含む）
- [ ] 日本語のフォントが正しく表示されているか

---

## 注意事項

- `.env.local` は必ず `.gitignore` に含める
- APIキーはコードに直書きしない。必ず環境変数を使う
- コミットメッセージは日本語で書く
- 作業の区切りごとに Git コミットを行う
- 母体プロジェクト：Gio（16_Gio）配下に独立フォルダとして配置
